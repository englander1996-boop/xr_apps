# morse

モールス信号一覧 (A-Z + 0-9 = 36 個)。

## できること

`A → .-` のように 1 文字ずつ表示。`Reveal` トグルで答えを隠せる。

## 操作

| 入力 | 動作 |
|---|---|
| グラス タップ | 次の文字 |
| グラス ダブルタップ | 答え表示のトグル |
| ブラウザ Next / Reveal | 同上 |

## 起動

```powershell
xr morse
```
