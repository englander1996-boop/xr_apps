# regex

正規表現の主要トークン 19 個 (`\d`, `\b`, `(?:...)`, `(?<=...)`, lazy 量指定子等)。

## できること

`(?=...) / positive lookahead` みたいにトークンと意味を 1 枚ずつ表示。

## 操作

| 入力 | 動作 |
|---|---|
| グラス タップ | 次へ |
| グラス ダブルタップ | ランダムジャンプ |
| ブラウザ Next / Random | 同上 |

## 起動

```powershell
xr regex
```
