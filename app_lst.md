# xr_apps アプリ一覧

`xr <name>` で起動できる自作 Even G2 アプリの一覧。
共通操作: **タップ** = 主アクション、**ダブルタップ** = リセット/裏アクション。詳細は各アプリの `app.json` / `src/main.ts` 参照。

ブラウザ側パネル (Vite が `http://localhost:5173/` で出すやつ) には主アクションのボタンと、入力が必要なものは設定 UI、最下段に Event Log が出る。これは `_lib/preview.ts` が用意する共通骨格。

---

## 🕐 時間系

| アプリ | 説明 |
|---|---|
| `pomodoro` | 25 分作業 / 5 分休憩のクラシック・ポモドーロ。タップで開始↔一時停止、ダブルでリセット。完了サイクル数を localStorage に保存。 |
| `hiit` | 20s on / 10s off × 8 ラウンドの HIIT (Tabata) インターバルタイマー。 |
| `stopwatch` | ストップウォッチ + ラップ。タップ = スタート / ラップ記録、ダブル = 停止＋リセット。表示は 1/100 秒精度。 |
| `multitimer` | 並列 3 タイマー (パスタ・茹で卵・お茶など同時調理用)。タップ = 選択枠を進める、ダブル = 選択枠のスタート/停止。各枠は名前と秒数を設定可能。 |
| `deadline` | 任意の日付までのカウントダウン。「公開まであと X日 Y時間 Z分」を 1 分ごとに更新。ラベルと日付はブラウザ側で設定。 |
| `lifesec` | 自分の DOB から積み上げた経過秒/分/時/日/年を表示。タップで単位切替。1 秒ごとに更新するので「生きてるカウンタ」として味あり。 |
| `wareki` | 西暦 ↔ 和暦変換器 (令和/平成/昭和/大正/明治)。タップで年を +1、ダブルで -1。ブラウザで任意年も直接入力できる。 |
| `lastsince` | 「最終 X からの経過」。3 種 (Coffee / Water / Stretch 等) を切替表示。タップ = 次の項目、ダブル = いま実行 (タイマーリセット)。 |

## 🎮 ゲーム系

| アプリ | 説明 |
|---|---|
| `numguess` | あなたが思い浮かべた 1-100 の数字を二分探索で当てる。タップ = もっと大きい、ダブル = もっと小さい。試行回数表示。 |
| `mathdrill` | 暗算ドリル。+ / - / × / ÷ をランダム出題、タップで答え表示 → 次へ。スコア集計あり。 |
| `reaction` | 反射神経測定。スタート → ランダム待機 → 「GO」表示でタップ、ms 単位で計測。早押しは「Too early!」でペナルティ。 |
| `hilow` | カードゲーム Hi-Low。1-13 が出て、タップ = 次は大きい、ダブル = 次は小さい、を当てる連勝チャレンジ。 |
| `janken` | じゃんけん。タップで自分の手 (Rock→Paper→Scissors) を切替、ダブルでコミット → コンピュータの手が出て判定。W/L/T 集計。 |
| `textadv` | 短いテキストアドベンチャー (暗い洞窟で目覚めるやつ)。各ノード 2 択。タップ = A、ダブル = B。エンディング数種類。 |
| `simon` | 記憶ゲーム Simon の単タップ版。S = タップ、D = ダブルの順番を覚えて再現。ラウンドごとに 1 ステップ追加。 |
| `mole` | モグラ叩き 1 ボタン版。「待て」中にタップするとミス、「MOLE」表示中にタップすると得点。反応時間と判断力両方使う。 |
| `g2048` | 4×4 のクラシック 2048。タップ = →、ダブル = ←、scroll-up = ↑、scroll-down = ↓ で 4 方向操作。全方向を「左スライドに正規化」して実装。ベストスコア保存。 |
| `hangman` | 英単語当てゲーム。scroll でアルファベットカーソル移動、タップで確定、ダブルで新ゲーム。40 語内蔵、ミス 6 回で負け。 |
| `wordle` | 5 文字 6 ターンの Wordle。スロット間 scroll 移動、各スロットで tap=次の文字 / double=前の文字、最終スロット超えで SUBMIT に到達。色がないので `X/./_` 記号で正解/位置違い/含まずを表現。 |
| `minesweeper` | 4×4 マインスイーパー (地雷 3、フラグなし)。scroll = カーソル縦、tap = 横 (折返し)、double = 開く。連鎖開示は BFS で実装。 |

## 💪 健康・身体系

| アプリ | 説明 |
|---|---|
| `breathing` | ボックスブリージング (4-4-4-4)。Inhale/Hold/Exhale/Hold の各フェーズと残秒、進捗バーをグラスに常時表示。 |
| `meditation` | 瞑想タイマー 5/10/15/20 分。タップでプリセット切替、ダブルでスタート/ストップ。終了時 *** DONE *** 表示。 |
| `sitalert` | 座りすぎアラート。30/45/60/90/120 分から選択。経過時間がリミット超えるとグラスに ** STAND UP ** 表示。タップでリセット (立った宣言)。 |
| `water` | 水分補給ロガー。タップ = +1 杯、ダブル = undo。今日の杯数と最後に飲んだ時刻、目標 (デフォルト 8 杯) に対する進捗バー表示。 |
| `toothbrush` | 2 分歯磨きタイマー。Upper-Left / Upper-Right / Lower-Left / Lower-Right を 30 秒ずつガイド。終了時 *** DONE ***。 |
| `neckstretch` | 首ストレッチガイド。左 / 中央 / 右 / 中央 / 上 / 下 を 15 秒ずつ。デスクワーク疲れ対策。 |
| `dbmeter` | マイクで取れる PCM の RMS から dBFS を計算してリアルタイム表示。閾値 -60 〜 0 dB をバーグラフ化。タップでマイクをオープン/クローズ。 |

## 📚 リファレンス・教育系

| アプリ | 説明 |
|---|---|
| `nato` | NATO フォネティックアルファベット (A=Alpha … Z=Zulu) 26 個。タップ = 次、ダブル = 表示トグル、ランダムボタンで暗記練習。 |
| `morse` | モールス信号一覧 (A-Z + 0-9)。タップ = 次、ダブル = 表示トグル。 |
| `periodic` | 周期表 全 118 元素 (原子番号 + 記号 + 英語名)。タップ = 次、ダブル = ランダム。 |
| `capitals` | 世界の首都暗記フラッシュカード約 53 か国。タップ = 答え表示 → 次、ダブル = スキップ。 |
| `pi` | 円周率の桁を 1 桁ずつ増やしながら表示。タップ = +1 桁、ダブル = -1 桁。約 500 桁分を内蔵。現在表示桁数は保存される。 |
| `vimcheat` | Vim のよく忘れるショートカット 20 個 (ciw / di" / qa…@a 等)。タップ = 次、ダブル = ランダム。 |
| `gitcheat` | Git の救命コマンド 18 個 (reflog / bisect / stash / cherry-pick / force-with-lease 等)。 |
| `httpcode` | HTTP ステータスコード 24 個 (100 / 200 / 301 / 404 / 418 / 429 / 502 …)、各説明付き。 |
| `regex` | 正規表現の主要トークン 19 個 (\\d / \\b / (?:...) / (?<=...) / 量指定子 / lazy 等)。 |
| `eitan` | 英単語フラッシュカード 40 語 (ubiquitous, ephemeral, exacerbate, ...)、和訳付き。タップ = 答え表示 → 次。 |
| `kanji` | 難読漢字 20 個 (薔薇/檸檬/憂鬱/麒麟/朧月夜 …)、読みと意味。タップ = 答え表示 → 次。 |

## 🎲 軽い遊び・生活系

| アプリ | 説明 |
|---|---|
| `omikuji` | おみくじ。タップで 1 枚引く (大吉〜大凶の 7 段階、短文の助言付き)。 |
| `dice` | サイコロ。タップ = d6、ダブル = d20。ブラウザ側に d4/d8/d10/d12/d100 のボタンもあり。直近 5 投の履歴表示。 |
| `horoscope` | 日替わり星占い (オフライン、サインと日付からハッシュで決定論的)。タップで 12 星座切替。星座は保存される。 |
| `quote` | 名言ローテーション 16 件 (Steve Jobs / Einstein / 日本のことわざ等)。タップ = 次、ダブル = ランダム。 |
| `counter` | タリーカウンタ。タップ = +1、ダブル = リセット。在庫数え、人数カウント、回数カウント用。ラベルとカウント値は保存される。 |
| `gomi` | 今日のゴミの日表示。曜日ごとに「燃える/プラ/缶ビン/...」を設定すると、今日と明日の分類を表示。 |
| `coffeelog` | コーヒー杯数ログ。タップ = +1、ダブル = undo。今日の杯数と直近 7 日平均を表示。日次自動リセット。 |

## 🌐 ネット系 (無 OAuth)

| アプリ | 説明 |
|---|---|
| `cryptoprice` | 仮想通貨価格ティッカー。CoinGecko 無料 API から BTC/ETH/SOL を 60s ごと取得。タップで通貨切替。 |
| `fxrate` | 為替レート (USD ベース → JPY/EUR/GBP/CNY/KRW)。ECB 公式の `api.frankfurter.dev` を 1 時間ごとに取得。タップでペア切替。ECB は日次更新なので頻繁にポーリングする必要なし。 |
| `wikitoday` | 英語 Wikipedia の「On This Day」API でその日の出来事を取得。タップで同カテゴリ内の次項目、ダブルでカテゴリ切替 (selected/events/births/deaths/holidays)。 |
| `hnews` | Hacker News トップ 20 記事。 `topstories.json` で id 一覧 → `item/<id>.json` を `Promise.all` で並列取得。10 分ごと自動更新。 |

---

## 共通アーキテクチャ

すべてのアプリは `xr_apps/_lib/` の共通ヘルパに依存する：

- **`_lib/even.ts`** — `createEvenApp()` でブリッジ接続 (4 秒タイムアウト、失敗時は preview-only モード)、自動で 1×1 不可視 List をイベント sink として設置、`OsEventTypeList.fromJson` で正規化、250ms デバウンス、`on('click'|'double'|'up'|'down', handler)` API、`app.audio.open/close/onPcm` でマイク制御
- **`_lib/preview.ts`** — ブラウザ側ミニ UI 骨格 (タイトル/状態/ボタン群/コンテンツ/Event Log のダーク UI を共通生成)
- **`_lib/storage.ts`** — localStorage の JSON 読み書き
- **`_lib/net.ts`** — `fetchJson<T>(url, log)` で HTTP 取得 + タイムアウト + 詳細ログ (URL / ステータス / バイト数 / 経過 ms / 失敗理由) を統一。ネット系アプリ全部これを使う
- 各アプリの `src/main.ts` は実質ロジック部分のみ (50-150 行)。`package.json` / `app.json` / `index.html` / `.gitignore` の 4 ファイルはほぼテンプレ

`_lib/` 配下に `package.json` を置いて `@evenrealities/even_hub_sdk` を 1 回インストールしてあるので、各アプリ側は SDK の型/値を `_lib` 経由で間接的に使う。 `xr <name>` で起動すると even-dev の Vite が `fs.allow` 経由で `_lib` まで配信してくれる (`xr_apps/` は各アプリ親として allow される)。

## 起動

```powershell
xr <name>        # 例: xr pomodoro
xr -List         # 全アプリ一覧
xr               # 対話選択
```

初回起動時に各アプリの `node_modules` が自動インストールされる (`run.ps1` 経由)。

## ハマったときに

- ブラウザ側 Event Log カード (一番下) を確認: `event kind=… source=… container=…` で 1 ジェスチャあたり 4 件くらいスパムされてれば bridge は届いている
- グラスでタップが効かない → `hello` の README を参照。 `_lib` 側で対策済みだが念のため
- 何も表示されない → `xr <name>` 初回起動時の `npm install` が失敗してる可能性。 `cd <name> && npm install` で手動再実行
